version: '2'
services:
  wii-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/wii/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=wii
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
